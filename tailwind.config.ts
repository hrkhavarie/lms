import type { Config } from "tailwindcss";

// const tailwindColor:{[key:string] : string} = {

// }

const config: Config = {
  content: [ 
    "./src/app/**/*.{tsx}",
  ],
  theme: {
    extend: {
     
    },
    },
  plugins: [],
};
export default config;
